server:
  port: 8090

spring:
  application:
    name: spring_batch_prac

  profiles:
    active: dev
    include: secret

  threads:
    virtual:
      enabled: true

  datasource:
    url: jdbc:h2:./db_dev;MODE=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_batch_fetch_size: 100
        format_sql: true
        use_sql_comments: true
        highlight_sql: true

  security:
    oauth2:
      client:
        registration:
          kakao:
            #            client-id: ON_SECRET
            scope: profile_nickname, profile_image
            client-name: Kakao
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            client-authentication-method: client_secret_post
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: ${spring.servlet.multipart.max-file-size}

logging:
  level:
    io.devground.spring_batch_prac: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
    org.hibernate.orm.jdbc.extract: TRACE
    org.springframework.transaction.interceptor: TRACE
    org.springframework.security: DEBUG
    org.springframework.security.oauth2: TRACE

custom:
  site:
    name: mb.onep.shop
    baseUrl: "http://localhost:${server.port}"

  temp:
    dirPath: C:/Users/river/Devcourse/Spring/mb/temp
  genFile:
    dirPath: C:/Users/river/Devcourse/Spring/mb

  toss-payments:
    targetUrl: https://api.tosspayments.com/v1/payments/confirm
